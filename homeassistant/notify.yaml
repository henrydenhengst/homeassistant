# =====================
# Telegram notificatie (Primary)
# =====================
telegram_primary:
  platform: telegram
  api_key: !secret telegram_token
  chat_id: 8255991553   # alleen nummer, geen @ of quotes nodig

# =====================
# Email notificatie (Backup)
# =====================
email_backup:
  platform: smtp
  server: smtp.gmail.com
  port: 587
  sender: henrydenhengst@gmail.com
  encryption: starttls
  username: henrydenhengst@gmail.com
  password: !secret email_password
  recipient:
    - henrydenhengst@gmail.com
    - ilonadenhengst@gmail.com

# =====================
# Gotify notificatie (Extra)
# =====================
gotify_notify:
  platform: rest
  name: gotify
  resource: "http://${JUST_IP}:8137/message"
  method: POST
  headers:
    Content-Type: "application/json"
  payload: '{"title": "{{ title }}", "message": "{{ message }}", "priority": 5, "token": "{{ token }}"}'